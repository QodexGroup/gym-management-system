# This file was auto-generated for Firebase Hosting PR Preview
# Automatically builds and deploys Pull Request branches to Firebase Hosting preview channels

name: Deploy to Firebase Hosting on PR
on:
  pull_request_target:
    types: [opened, synchronize, reopened, closed]
    branches:
      - master

permissions:
  contents: read
  checks: write
  pull-requests: write
  statuses: write

jobs:
  build_and_preview:
    # Skip deployment if PR is closed
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write
      statuses: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - run: npm ci
      
      - name: Secure Config Injection and Build
        env:
          FIREBASE_ADMIN_CREDENTIALS: ${{ secrets.FIREBASE_ADMIN_CREDENTIALS }}
          VITE_API_BASE_URL: https://gym-management-api-21018123230.asia-southeast1.run.app/api
        run: |
          echo "$FIREBASE_ADMIN_CREDENTIALS" > ./service-account-key.json
          export GOOGLE_APPLICATION_CREDENTIALS="./service-account-key.json"
          npm run build
          rm ./service-account-key.json
      
      - name: Deploy to Firebase Hosting Preview
        id: firebase-deploy
        env:
          FIREBASE_ADMIN_CREDENTIALS: ${{ secrets.FIREBASE_ADMIN_CREDENTIALS }}
        run: |
          set -e
          npm install -g firebase-tools
          
          # Verify dist folder exists
          if [ ! -d "dist" ]; then
            echo "ERROR: dist folder does not exist!"
            exit 1
          fi
          
          # Verify firebase.json exists and is valid
          if [ ! -f "firebase.json" ]; then
            echo "ERROR: firebase.json does not exist!"
            exit 1
          fi
          
          # Set up authentication
          echo "$FIREBASE_ADMIN_CREDENTIALS" > ./service-account-key.json
          export GOOGLE_APPLICATION_CREDENTIALS="./service-account-key.json"
          
          # Create .firebaserc file
          echo '{"projects":{"default":"gymhubtech-67e6f"}}' > .firebaserc
          
          # Verify Firebase can see the project
          firebase projects:list --json || echo "Project list check completed"
          
          # Deploy to preview channel using hosting:channel:deploy command
          CHANNEL_ID="pr-${{ github.event.pull_request.number }}"
          echo "Deploying to channel: $CHANNEL_ID"
          
          # Use hosting:channel:deploy which is specifically for preview channels
          firebase hosting:channel:deploy $CHANNEL_ID --project gymhubtech-67e6f --only hosting
          
          # Construct preview URL
          PREVIEW_URL="https://gymhubtech-67e6f--${CHANNEL_ID}.web.app"
          echo "PREVIEW_URL=$PREVIEW_URL" >> $GITHUB_OUTPUT
          echo "Preview URL: $PREVIEW_URL"
          
          rm ./service-account-key.json || true
      
      - name: Comment PR with Preview URL
        uses: actions/github-script@v7
        if: steps.firebase-deploy.outputs.PREVIEW_URL != ''
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const previewUrl = '${{ steps.firebase-deploy.outputs.PREVIEW_URL }}';
            const prNumber = context.payload.pull_request.number;
            
            const comments = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });
            
            const botComment = comments.data.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('🚀 **Frontend Preview Deployment**')
            );
            
            const body = `🚀 **Frontend Preview Deployment**
            
            ✅ Your frontend has been deployed to a preview channel!
            
            **Preview URL:** ${previewUrl}
            
            This preview will be automatically deleted when the PR is closed or merged.
            
            ---
            *Deployed from commit: \`${{ github.sha }}\`*`;
            
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: body
              });
            }
